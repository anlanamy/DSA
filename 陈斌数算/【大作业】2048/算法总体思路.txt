极小化极大算法思路
根据自己定义的value函数，计算棋盘对于己方的价值。每一次轮到自己下棋时，尽可能在下一步取得更大的价值，而轮到对手下棋的时候，尽可能在下一步取得更小的价值。
alpha-beta剪枝
alpha初始化为一个很小的值，表示的是之前搜索到的己方价值最大值，根据搜索结果逐渐增大，beta初始化为一个很大的值，表示的是之前搜索到的对方价值最小值，根据搜索结果逐渐减小，如果alpha>=beta，说明这段搜索树的结果不可能最终被采纳，则剪枝。